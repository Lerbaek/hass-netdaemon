namespace Lerbaek.Test.Common.Bases.Startup
{
  public abstract class LoggerTestsStartup
  {
    //public void Configure(ILoggerFactory loggerFactory, ITestOutputHelperAccessor accessor) =>
    //  loggerFactory.AddProvider(new XunitTestOutputLoggerProvider(accessor, (_, level) => level >= LogLevel.Debug));

    //public void ConfigureServices(IServiceCollection services)
    //{
    //  services.AddLogging(loggingBuilder => loggingBuilder.AddXUnit();
    //}
  }
}